{% render 'section.flex-pdp.description', product: closest.product %}

{% schema %}
{
  "name": "t:flex_pdp.blocks.description.name",
  "presets": [
    {
      "name": "t:flex_pdp.blocks.description.name"
    }
  ]
}
{% endschema %}