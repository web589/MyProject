{% render 'section.flex-pdp.price', product: closest.product %}

{% schema %}
{
  "name": "t:flex_pdp.blocks.price.name",
  "presets": [
    {
      "name": "t:flex_pdp.blocks.price.name"
    }
  ]
}
{% endschema %}