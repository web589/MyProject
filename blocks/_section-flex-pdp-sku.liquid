{% render 'section.flex-pdp.sku', product: closest.product %}

{% schema %}
{
  "name": "t:flex_pdp.blocks.sku.name",
  "presets": [
    {
      "name": "t:flex_pdp.blocks.sku.name"
    }
  ]
}
{% endschema %}