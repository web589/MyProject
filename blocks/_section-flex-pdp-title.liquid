{% render 'section.flex-pdp.title', product: closest.product %}

{% schema %}
{
  "name": "t:flex_pdp.blocks.title.name",
  "presets": [
    {
      "name": "t:flex_pdp.blocks.title.name"
    }
  ]
}
{% endschema %}