{% render 'section.flex-pdp.vendor', product: closest.product %}

{% schema %}
{
  "name": "t:flex_pdp.blocks.vendor.name",
  "presets": [
    {
      "name": "t:flex_pdp.blocks.vendor.name"
    }
  ]
}
{% endschema %}